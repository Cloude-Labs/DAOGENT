"homepage": "https://your-username.github.io/DAOGENT",
"scripts": {
  "build": "next build",
  "export": "next export",
  "deploy": "gh-pages -d out",
  "predeploy": "npm run clean && npm run build && npm run export", // Updated predeploy script to include clean
  "clean": "rm -rf out" // Added clean script to remove old build files
}
